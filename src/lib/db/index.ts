/**
 * Persistence (02-DATA-MODEL): Dexie schema (`schema.ts`), typed repository (`repo.ts`) and
 * reactive reads (`live.svelte.ts`). Components never call Dexie directly. Filled in M1.
 */

export {}
